package com.lucap.scubakeep;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Verifies the Spring Boot application context starts correctly.
 * If the context fails to load due to misconfigured beans, missing dependencies,
 * or invalid configuration, this test will fail.
 */
@SpringBootTest
class ScubaKeepApplicationTest {

    @Test
    void contextLoads() {
    }

}
